:php
  /**
   *
   * Chimera template part for displaying page head
   *
   * @package WordPress
   * @subpackage Chimera
   * @since Chimera 1.0
   */

   $args = array(
     'orderby' => 'rand',
     'posts_per_page' => 6,
     'post_type' => array('slide')
   );

   $parts = new WP_Query( $args );

section.home-slideshow.clear
  .sw.m0.poxy.a_12.b_12.c_58.d_58.e_p11.ofh
    slick.mb0
      :php
        while ($parts->have_posts()) : $parts->the_post();
          get_template_part('part-slide');
        endwhile;
        wp_reset_query();
